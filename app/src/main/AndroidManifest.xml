<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.akdsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Blog.NewBlogsActivity"></activity>
        <activity android:name=".Message.MessageRoomActivity" />
        <activity android:name=".Tests.TestsActivity" />
        <activity android:name=".Register.RegisterActivity" />
        <activity android:name=".Adds.AddsActivity" />
        <activity android:name=".Blog.BlogActivity" />
        <activity android:name=".Login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Message.MessageActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>